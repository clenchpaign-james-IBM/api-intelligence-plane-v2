# Frontend Environment Variables

# API Base URL - Leave empty to use Vite proxy (recommended for Docker)
# Set to http://localhost:8000 only when running frontend standalone
VITE_API_BASE_URL=

# Example for standalone development:
# VITE_API_BASE_URL=http://localhost:8000