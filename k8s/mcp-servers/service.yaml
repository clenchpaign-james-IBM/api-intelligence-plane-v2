apiVersion: v1
kind: Service
metadata:
  name: mcp-unified
  namespace: api-intelligence-plane
  labels:
    app: mcp-unified
    component: mcp
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: mcp-unified

# Made with Bob
