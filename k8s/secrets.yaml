# Secrets Configuration for API Intelligence Plane
# NOTE: This file contains placeholder values. Replace with actual secrets before deployment.
# For production, use external secret management (e.g., Sealed Secrets, External Secrets Operator, Vault)

apiVersion: v1
kind: Secret
metadata:
  name: opensearch-credentials
  namespace: api-intelligence-plane
type: Opaque
stringData:
  admin-password: "CHANGE_ME_OPENSEARCH_PASSWORD"
---
apiVersion: v1
kind: Secret
metadata:
  name: llm-credentials
  namespace: api-intelligence-plane
type: Opaque
stringData:
  openai-api-key: "CHANGE_ME_OPENAI_API_KEY"
  anthropic-api-key: "CHANGE_ME_ANTHROPIC_API_KEY"
  azure-api-key: "CHANGE_ME_AZURE_API_KEY"

# Made with Bob
